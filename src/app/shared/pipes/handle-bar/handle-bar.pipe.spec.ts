import { HandleBarPipe } from './handle-bar.pipe';

describe('HandleBarPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleBarPipe();
    expect(pipe).toBeTruthy();
  });
});
