import { ContentFormatterPipe } from './content-formatter.pipe';

describe('ContentFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
