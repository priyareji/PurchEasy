import { RangeDateTimePipe } from './range-date-time.pipe';

describe('RangeDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RangeDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
