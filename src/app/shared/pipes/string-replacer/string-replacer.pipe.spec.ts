import { StringReplacerPipe } from './string-replacer.pipe';

describe('StringReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
