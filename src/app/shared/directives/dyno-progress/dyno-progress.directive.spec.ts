import { DynoProgressDirective } from './dyno-progress.directive';

describe('DynoProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new DynoProgressDirective();
    expect(directive).toBeTruthy();
  });
});
