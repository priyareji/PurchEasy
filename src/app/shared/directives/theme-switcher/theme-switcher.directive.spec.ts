import { ThemeSwticherDirective } from './theme-switcher.directive';

describe('ThemeSwticherDirective', () => {
	it('should create an instance', () => {
		const directive = new ThemeSwticherDirective();
		expect(directive).toBeTruthy();
	});
});
