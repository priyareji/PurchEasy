import { ViewportAnalyzerDirective } from './viewport-analyzer.directive';

describe('ViewportAnalyzerDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportAnalyzerDirective();
    expect(directive).toBeTruthy();
  });
});
